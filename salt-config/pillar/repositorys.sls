configuration:
  repos:

# Local repository for Salt, leaving this blank will default
# to 'http://repo.saltstack.com/apt/ubuntu/16.04/amd64/latest'

    salt:
      url: 'http://storage.blacknet.lan/salt'
      key: 'http://storage.blacknet.lan/salt/key/SALTSTACK-GPG-KEY.pub'
